[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rsna-anonymizer"
version = "19.0.6"
description = "RSNA DICOM Anonymizer"
readme = "readme.md"
license = { text = "RSNA-MIRC Public License" }
authors = [{ name = "Algorhythm Software", email = "michael@dx.life" }]
requires-python = ">=3.11.0,<3.13.0"
dependencies = [
    "numpy<2.0.0",
    "customtkinter",
    "pillow",
    "tkhtmlview",
    "pydicom>=2.4,<2.5",
    "pynetdicom",
    "ifaddr",
    "boto3",
    "openpyxl",
    "psutil",
    "pylibjpeg[all]",
    "opencv-python-headless",
    "easyocr",
    "torch",
    "torchvision",
    "cryptography",
    "requests",
    "requests-oauthlib",
    "toml>=0.10.2",
    "click>=8.1.8",
    "dataclasses-json>=0.6.7",
    "bidict>=0.23.1",
    "sqlalchemy>=2.0.38",
    "simpleitk>=2.5.5",
    "scipy",
    "totalsegmentator>=2.3.0",
    "xgboost>=2.0.0",
    "nibabel",
    "dicom2nifti>=2.3,<2.6",
    "tkinterdnd2>=0.6.3",
]

[project.scripts]
rsna-anonymizer = "anonymizer.anonymizer:main"

[project.urls]
Homepage = "https://github.com/RSNA/anonymizer"

[tool.hatch.build.targets.wheel]
packages = ["src/anonymizer"]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux'" }]
torchvision = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux'" }]

[dependency-groups]
dev = [
    "pre-commit",
    "ruff",
    "black",
    "pytest",
    "pytest-cov>=6.0.0",
    "python-dotenv",
    "ffmpeg-python>=0.2.0",
    "pytest-mock>=3.14.1",
    "watchfiles>=1.0.0",
    "setuptools>=70,<82",
]
docs = [
    "mkdocs-material>=9.5.0",
    "mkdocs-static-i18n>=1.2.0",
]
prototyping-viz = [
    "matplotlib>=3.8.0",
    "scikit-image>=0.22.0",
]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = [
    "E",
    "F",
    "W",
    "I",
    "B",
    "SIM",
    "C90",
]
fixable = [
    "I001",
    "W292",
    "W293",
    "W291",
    "F401",
]
ignore = [
    "E501",
    "SIM102",
    "C901",
]

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.pytest.ini_options]
testpaths = ["tests/controller", "tests/view", "tests/model"]
addopts = "--verbose --cov=src/anonymizer/controller --cov=src/anonymizer/utils --cov=src/anonymizer/model -m \"not ocr_integration and not view_dev\""
pythonpath = ["src", "."]
filterwarnings = [
    # pydicom 2.4 still loads packaged testdata via deprecated pkg_resources entry points.
    "ignore:pkg_resources is deprecated as an API:UserWarning:pydicom.data.data_manager",
]
markers = [
    "tseg_integration: run TotalSegmentator inference (slow, optional)",
    "ocr_integration: EasyOCR on real burnt-in PHI fixtures under tests/controller/ocr (opt-in; excluded from default)",
    "dicom_integration: local Orthanc PACS or heavy DICOM network integration (skipped in CI)",
    "rsna_local_data: requires local RSNA_TEST_DATA directory (skipped when absent)",
    "view_dev: local Series View layout tests against developer MRI_TEST fixtures (excluded from CI)",
    "prototyping: tests for src/prototyping scripts",
]
